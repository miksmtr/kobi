import 'package:flutter/material.dart';


class FirestoreDataLoading extends StatelessWidget {
  const FirestoreDataLoading({super.key,});

  @override
  Widget build(BuildContext context) {
    return const Text('Loading users...');
  }
}
