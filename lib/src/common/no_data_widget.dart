import 'package:flutter/material.dart';

class FirestoreNoData extends StatelessWidget {
   const FirestoreNoData({super.key,});

  @override
  Widget build(BuildContext context) {
    return Text('No Data!');
  }
}
